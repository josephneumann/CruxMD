/**
 * Clinical components barrel export.
 * These components render clinical data from the agent's structured responses.
 */
export { InsightCard } from "./InsightCard";
export type { InsightCardProps } from "./InsightCard";
